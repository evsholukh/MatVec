{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug OpenCL",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}\\example.exe",
            "cwd": "${workspaceFolder}",
            "MIMode": "gdb",
            "preLaunchTask": "Build"
        },
    ]
}