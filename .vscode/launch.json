{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug OpenCL",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}\\cl_example.exe",
            "cwd": "${workspaceFolder}",
            "MIMode": "gdb",
            "preLaunchTask": "Build OpenCL"
        }
    ]
}